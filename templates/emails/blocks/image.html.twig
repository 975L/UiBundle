{# v1: a plain absolute image URL (paste one from the Media library) rather than a Media picker - see EmailBlock docblock #}
<tr>
    <td style="padding:0 24px 24px;">
        <img src="{{ url }}" alt="{{ alt }}" width="552" style="display:block;width:100%;max-width:552px;height:auto;border:0;outline:none;text-decoration:none;">
    </td>
</tr>
