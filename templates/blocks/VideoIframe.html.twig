{# Adapter for the dynamic Block system — maps Block data to the Video:Iframe component #}
<twig:c975LUi:Video:Iframe src="{{ src }}" title="{{ title|default('') }}" description="{{ description|default('') }}" width="{{ width }}" height="{{ height }}" class="{{ class }}"/>
