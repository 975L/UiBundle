{# Adapter for the dynamic Block system — maps Block data to the Section:Cards component #}
<twig:c975LUi:Section:Cards eyebrow="{{ eyebrow|default('') }}" title="{{ title|default('') }}" cards="{{ cards|default([]) }}"/>
