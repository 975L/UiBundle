{# Adapter for the dynamic Block system — maps Block data to the Progress:Bar component #}
<twig:c975LUi:Progress:Bar label="{{ label|default('') }}" progressPercent="{{ progressPercent }}" text="{{ text }}"/>
