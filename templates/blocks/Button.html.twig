{# Adapter for the dynamic Block system — maps Block data to the Button component #}
<twig:c975LUi:Button:Button label="{{ label }}" url="{{ url }}" type="{{ type }}" target="{{ target }}" icon="{{ icon }}" download="{{ download }}" inline="{{ inline }}"/>
