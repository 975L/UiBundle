{# Adapter for the dynamic Block system — maps Block data/media to the Slider component #}
<twig:c975LUi:Slider:Slider media="{{ block.media }}" id="{{ id }}" duration="{{ duration }}" ratio="{{ ratio }}" layout="{{ layout }}" class="{{ class }}"/>
