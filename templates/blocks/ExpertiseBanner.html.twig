{# Adapter for the dynamic Block system — maps Block data to the Expertise:Banner component #}
<twig:c975LUi:Expertise:Banner eyebrow="{{ eyebrow|default('') }}" title="{{ title }}" text="{{ text|default('') }}" tags="{{ tags|default([]) }}"/>
