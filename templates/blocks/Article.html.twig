{# Adapter for the dynamic Block system — maps Block data/media to the Article component #}
<twig:c975LUi:Article:Article title="{{ title }}" hook="{{ hook }}" content="{{ content }}" mediaList="{{ block.media }}" id="article-{{ block.data.slug|default('') }}" uid="{{ block.id }}"/>