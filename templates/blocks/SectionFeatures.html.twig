{# Adapter for the dynamic Block system — maps Block data to the Section:Features component #}
<twig:c975LUi:Section:Features eyebrow="{{ eyebrow|default('') }}" title="{{ title|default('') }}" cards="{{ cards|default([]) }}" id="{{ anchor_id }}"/>
