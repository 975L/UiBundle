{# Adapter for the dynamic Block system — maps Block data to the Cta:Band component #}
<twig:c975LUi:Cta:Band title="{{ title }}" text="{{ text|default('') }}" ctaLabel="{{ ctaLabel }}" ctaUrl="{{ ctaUrl }}" id="{{ anchor_id }}"/>
