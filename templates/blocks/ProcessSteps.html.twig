{# Adapter for the dynamic Block system — maps Block data to the Process:Steps component #}
<twig:c975LUi:Process:Steps eyebrow="{{ eyebrow|default('') }}" title="{{ title }}" steps="{{ steps|default([]) }}"/>
