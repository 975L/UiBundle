{# Adapter for the dynamic Block system — maps Block data to the Video:Video component #}
<twig:c975LUi:Video:Video src="{{ src }}" type="{{ type }}" poster="{{ poster }}" autoplay="{{ autoplay }}" muted="{{ muted }}" width="{{ width }}" height="{{ height }}"/>
