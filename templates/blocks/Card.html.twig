{# Adapter for the dynamic Block system — maps Block data to the Card component #}
<twig:c975LUi:Card:Card id="{{ id }}" title="{{ title }}" level="{{ level }}" content="{{ content }}" class="{{ class }}"/>
